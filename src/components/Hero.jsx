import Search from "./Search";

const Hero = () => {
    return (
        <div className="background flex flex-col justify-center items-center px-4 md:px-0">
            <Search />
        </div>
    );
};

export default Hero;
