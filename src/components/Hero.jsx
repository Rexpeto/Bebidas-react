import Search from "./Search";

const Hero = () => {
    return (
        <div className="background flex flex-col justify-center items-center">
            <Search />
        </div>
    );
};

export default Hero;
